#### Code from the Next.js course presented by https://jsmastery.pro

```javascript
// MAJOR DEPENDENCIES

'Clerk + Svix' are ¹a user management library for React and ²a package designed to facilitate the verification of webhook signatures.

'MongoDB + Mongoose' are ¹a NoSQL database and ²an Object Data Modeling (ODM) library.

'Next.js + TypeScript' are ¹a React framework that enables the creation of server-rendered React applications and ²a superset of JavaScript that adds static typing to the language.

'React Hook Form' is a library for managing forms in React applications.
Requires: { '@hookform/resolvers' }

'Shadcn' is a Tailwind plugin for UI component templates.
Requires: { '@radix-ui', 'class-variance-authority' }

'Tailwind' is a utility-first CSS framework that simplifies the styling of web applications.
Requires: { 'autoprefixer', 'postcss' }

'Zod' is a TypeScript-first schema declaration and validation library. It provides a concise and expressive way to define data schemas and performs runtime validation of data against these schemas.

// MINOR DEPENDENCIES

'Classnames' is a JavaScript utility for conditionally joining classNames together.

'clsx' is a tiny utility for constructing className strings conditionally

'ESLint + Prettier' are ¹a static code analysis tool for identifying and fixing problematic patterns or code errors in JavaScript and TypeScript and ²a code formatting tool that automates the process of styling code according to a predefined set of rules.

'Lucide' is a library that provides a set of React components for using Lucide icons in your React applications.

'TinyMCE' is a web-based, open-source WYSIWYG (What You See Is What You Get) editor that simplifies the process of content creation.

```
